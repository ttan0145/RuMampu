# US2.1 验收记录：逐月查看收入

语言：[English](US2.1_MONTH_BY_MONTH.md) | **中文（CN）**

| 验收标准 | 状态 | 实现与证据 |
| --- | --- | --- |
| AC2.1.1 显示月度收入图 | 通过 | `build_income_pattern` 按日历月聚合当前访客收入；`IncomePatternChart` 渲染 typed response；空记录显示明确的录入入口。 |
| AC2.1.2 显示月份标签 | 通过 | 每个 bar 有可见 `MMM YY` 标签和包含该月计算金额的可访问标签。 |
| AC2.1.3 反映不同月度金额 | 通过 | 正数、零值和负数共用零线并按权威金额缩放；空状态不会产生 `NaN` 或 `Infinity`。 |

后端回归覆盖同月多来源、超过单笔记录位数上限的聚合值、共用收入模型中的历史总额、每月只扣一次工作成本、负可用收入和 0/1/2/N 月。Playwright 验证 12 个带年份的 bar、清晰的横向滚动提示、有效的 bar 高度差异，以及同时包含零值与负可用收入的有限历史图表。
